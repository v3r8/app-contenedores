[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "appcontenedores"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flet==0.24.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["appcontenedores*"]
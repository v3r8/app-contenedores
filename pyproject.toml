[project]
name = "app-contenedores"
version = "0.1.0"
description = "Aplicación para control y reporte de contenedores"
dependencies = [
    "flet>=0.28.0"
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"